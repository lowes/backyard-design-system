import styled from 'styled-components'

const ScrollbarTrackWrapper = styled.div`
    position: relative;
    background: var(--bds-color-surface-subdued);
    height: 4px;
    order: 10;
    width: 100%;
    border-radius: var(--bds-border-radius-circle);
`

export { ScrollbarTrackWrapper }

export default ScrollbarTrackWrapper
